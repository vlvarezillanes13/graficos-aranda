# --- Vercel / producción (server-only, sin prefijo VITE_) ---
AUTH_SESSION_SECRET=genera_un_secreto_largo_aleatorio

# Bootstrap para crear token de sesión ITSM (no es el token de sesión en sí)
ITSM_INTEGRATION_TOKEN=Bearer tu_token_de_integracion

# AUTH_ADMIN_PASSWORD=clave_personalizada_opcional

# --- Local opcional (fallback si no usas las variables de arriba) ---
# VITE_AUTH_SESSION_SECRET=genera_un_secreto_largo_aleatorio
# VITE_ITSM_INTEGRATION_TOKEN=Bearer tu_token_de_integracion

# Legacy (alias de ITSM_INTEGRATION_TOKEN):
# ITSM_AUTH_TOKEN=Bearer tu_token_de_integracion
